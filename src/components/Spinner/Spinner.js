import React from 'react';
import './Spinner.css'

const Spinner = () => {
    return (
        <>
            <div className="loader">Загрузка...</div>  
        </>
    );
};

export default Spinner;